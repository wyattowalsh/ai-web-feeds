name: Quality Enforcement

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write
  checks: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 🐍 Python Quality Checks
  python-formatting:
    name: Python - Formatting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@e16496da15ae13d37b4bd94bd25447904da7fc5a

      - name: Install uv
        uses: astral-sh/setup-uv@e9d0d9a1a81cd4e8ceef21e0eb11f15b4be0e5f0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318458e6f4ee6ba0924cf52375e
        with:
          python-version: '3.13'

      - name: Cache dependencies
        uses: actions/cache@13aacd865c20de30d75d3e0c0f8e4813aa9c821b
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/uv.lock') }}

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Check Ruff formatting
        run: uv run ruff format --check .
        id: ruff-format

      - name: Report formatting issues
        if: ${{ failure() && steps.ruff-format.outcome == 'failure' }}
        run: echo "::error::Python formatting check failed."

      - name: Add PR comment if formatting fails
        if: ${{ failure() && steps.ruff-format.outcome == 'failure' && github.event_name == 'pull_request' }}
        uses: actions/github-script@60a0d83039b000713aaf4c38b122b5342f57cdfc
        with:
          script: |
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ **Python Formatting Check Failed**\n\n' +
                    'Run locally:\n```bash\nuv run ruff format .\n```\n\n' +
                    'Or comment `/fix` to auto-fix.'
            });

  python-linting:
    name: Python - Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@e16496da15ae13d37b4bd94bd25447904da7fc5a

      - name: Install uv
        uses: astral-sh/setup-uv@e9d0d9a1a81cd4e8ceef21e0eb11f15b4be0e5f0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318458e6f4ee6ba0924cf52375e
        with:
          python-version: '3.13'

      - name: Cache dependencies
        uses: actions/cache@13aacd865c20de30d75d3e0c0f8e4813aa9c821b
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/uv.lock') }}

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run Ruff linting
        run: uv run ruff check . --output-format=github

  python-type-checking:
    name: Python - Type Checking
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@e16496da15ae13d37b4bd94bd25447904da7fc5a

      - name: Install uv
        uses: astral-sh/setup-uv@e9d0d9a1a81cd4e8ceef21e0eb11f15b4be0e5f0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318458e6f4ee6ba0924cf52375e
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run type checking (mypy)
        continue-on-error: true
        run: uv run mypy packages/ai_web_feeds/src apps/cli/ai_web_feeds tests
        id: mypy-check

      - name: Report type checking issues
        if: ${{ steps.mypy-check.outcome == 'failure' }}
        run: echo "::error::Type checking failed."

      - name: Add PR comment if type checking fails
        if: ${{ steps.mypy-check.outcome == 'failure' && github.event_name == 'pull_request' }}
        uses: actions/github-script@60a0d83039b000713aaf4c38b122b5342f57cdfc
        with:
          script: |
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ **Type Checking Failed**\n\nPlease fix type errors reported by mypy.\n\nRun `uv run mypy packages/ai_web_feeds/src apps/cli/ai_web_feeds tests` locally to see all errors.'
            });

  enforce-testing:
    name: Enforce Test Coverage (≥90%)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@e16496da15ae13d37b4bd94bd25447904da7fc5a

      - name: Install uv
        uses: astral-sh/setup-uv@e9d0d9a1a81cd4e8ceef21e0eb11f15b4be0e5f0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318458e6f4ee6ba0924cf52375e
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run tests with coverage (via CLI)
        run: |
          cd tests
          uv run pytest -q tests/cli tests/packages/ai_web_feeds/unit --ignore=tests/packages/ai_web_feeds/unit/test_nlp_entity_extractor.py --ignore=tests/packages/ai_web_feeds/unit/test_nlp_sentiment_analyzer.py --ignore=tests/packages/ai_web_feeds/unit/test_nlp_topic_modeler.py
          uv run coverage xml -o reports/coverage/coverage.xml

      - name: Check coverage threshold
        id: coverage-check
        run: |
          cd tests
          COVERAGE=$(uv run coverage report | grep TOTAL | awk '{print $4}' | sed 's/%//')
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT

          if [ -z "$COVERAGE" ]; then
            echo "::error::Coverage value could not be parsed from coverage report output."
            exit 1
          fi

          if (( $(echo "$COVERAGE < 90" | bc -l) )); then
            echo "::error::Coverage is $COVERAGE% (required: ≥90%)"
            exit 1
          else
            echo "::notice::Coverage is $COVERAGE% (required: ≥90%)"
          fi

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@50769540e7f4bd5e21e526ee35165aba89db6baf
        with:
          name: coverage-report
          path: tests/reports/coverage/
          retention-days: 7

      - name: Add PR comment with coverage
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@60a0d83039b000713aaf4c38b122b5342f57cdfc
        with:
          script: |
            const coverage = '${{ steps.coverage-check.outputs.coverage }}';
            const passed = parseFloat(coverage) >= 90;
            const emoji = passed ? '✅' : '❌';
            const status = passed ? 'PASSED' : 'FAILED';

            const message = `${emoji} **Coverage Check ${status}**

            Current coverage: **${coverage}%**
            Required: **≥90%**

            ${passed ? 'Great job maintaining high test coverage! 🎉' : 'Please add tests to meet the 90% coverage threshold.'}

            Run \`cd tests && uv run pytest\` to reproduce the coverage job locally.`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });

  enforce-data-validation:
    name: Enforce Canonical Data Assets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@e16496da15ae13d37b4bd94bd25447904da7fc5a

      - name: Install uv
        uses: astral-sh/setup-uv@e9d0d9a1a81cd4e8ceef21e0eb11f15b4be0e5f0
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@0b93645e9fea7318458e6f4ee6ba0924cf52375e
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Validate canonical data assets
        run: uv run python data/validate_data_assets.py

      - name: Add PR comment if validation fails
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@60a0d83039b000713aaf4c38b122b5342f57cdfc
        with:
          script: |
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ **Canonical Data Asset Validation Failed**\n\nPlease fix the canonical data asset validation errors, including any stale generated exports.\n\nRun `uv run python data/validate_data_assets.py` locally to see all errors.'
            });

  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs:
      - python-formatting
      - python-linting
      - python-type-checking
      - enforce-testing
      - enforce-data-validation
    if: always()

    steps:
      - name: Check all jobs
        run: |
          if [ "${{ needs.python-formatting.result }}" != "success" ] || \
             [ "${{ needs.python-linting.result }}" != "success" ] || \
             [ "${{ needs.python-type-checking.result }}" != "success" ] || \
             [ "${{ needs.enforce-testing.result }}" != "success" ] || \
             [ "${{ needs.enforce-data-validation.result }}" != "success" ]; then
            echo "::error::Quality gate detected one or more checks that did not pass."
            exit 1
          else
            echo "::notice::Quality gate passed - all checks successful!"
          fi

      - name: Add PR comment on quality gate status
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@60a0d83039b000713aaf4c38b122b5342f57cdfc
        with:
          script: |
            const formatting = '${{ needs.python-formatting.result }}';
            const linting = '${{ needs.python-linting.result }}';
            const typeCheck = '${{ needs.python-type-checking.result }}';
            const testing = '${{ needs.enforce-testing.result }}';
            const validation = '${{ needs.enforce-data-validation.result }}';

            const statusEmoji = (status) => status === 'success' ? '✅' : '❌';

            const allPassed = formatting === 'success' &&
                             linting === 'success' &&
                             typeCheck === 'success' &&
                             testing === 'success' &&
                             validation === 'success';

            const message = `## ${allPassed ? '✅' : '❌'} Quality Gate ${allPassed ? 'PASSED' : 'FAILED'}

            | Check | Status |
            |-------|--------|
            | Code Formatting | ${statusEmoji(formatting)} ${formatting} |
            | Linting | ${statusEmoji(linting)} ${linting} |
            | Type Checking | ${statusEmoji(typeCheck)} ${typeCheck} |
            | Testing (≥90%) | ${statusEmoji(testing)} ${testing} |
            | Canonical Data Assets | ${statusEmoji(validation)} ${validation} |

            ${allPassed
              ? '🎉 All quality checks passed! This PR is ready for review.'
              : '⚠️ Some quality checks failed. Please fix the issues above before merging.'}

            ---
            *Run \`uv run ruff check . --output-format=github\`, \`uv run ruff format --check .\`, \`uv run mypy packages/ai_web_feeds/src apps/cli/ai_web_feeds tests\`, \`uv run python data/validate_data_assets.py\`, and \`cd tests && uv run pytest -q tests/cli tests/packages/ai_web_feeds/unit --ignore=tests/packages/ai_web_feeds/unit/test_nlp_entity_extractor.py --ignore=tests/packages/ai_web_feeds/unit/test_nlp_sentiment_analyzer.py --ignore=tests/packages/ai_web_feeds/unit/test_nlp_topic_modeler.py\` locally to verify all checks before pushing.*`;

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: message
            });
