---
title: Product Overview
description: The main pages, workflows, and optional backend capabilities in AI Web Feeds.
icon: Sparkles
---

import { Card, Cards } from "fumadocs-ui/components/card";

ai-web-feeds is organized around one primary product surface: the canonical
reader at `/`. `/?mode=catalog` is the source-refinement view inside the same
workspace, `/dashboard` is the compact health view, and legacy product routes
redirect back to those two surfaces.

## Main Capabilities

<Cards>
  <Card title="Reader" description="Corpus-backed browsing, filtering, preview, and local reading state on /." href="/docs/features/reader" />
  <Card title="Search & Discovery" description="Unified article search, autocomplete, and explicit catalog mode for source discovery" href="/docs/features/search" />
  <Card title="Feed Management" description="Centralized YAML-based registry with schema validation" href="#feed-management" />
  <Card title="Advanced Fetching" description="Extract 100+ metadata fields with quality scoring" href="#advanced-fetching" />
  <Card title="Analytics" description="8 analytics views with health monitoring" href="#analytics" />
  <Card title="Platform Integration" description="Support for Reddit, GitHub, YouTube, and more" href="/docs/features/platform-integrations" />
  <Card title="CLI Tools" description="Corpus refresh/export workflows plus command-line data operations" href="/docs/development/cli" />
</Cards>

## Curated Catalog

1. Open `/` to browse the generated article corpus.
2. Switch to `/?mode=catalog` when you need to narrow or expand the source list.
3. Return to the reader on `/` with that slice preserved in the URL.
4. Use `/dashboard` for catalog health and keep OPML, enrichment, and analytics management in CLI workflows.

## Reading Surface

`/` is the main place to work with the catalog. It lets you:

- search the generated article library
- filter sources by topic, source type, and verification status
- switch between the reading view and the catalog view
- keep read, saved, starred, and archived state in the browser

## Export and Interop

`/api/exports/opml` gives you the full catalog in portable OPML formats:

- flat OPML for quick imports
- foldered OPML for readers that support grouped feeds
- filtered OPML from the current catalog slice

## Optional Backend

Most of the public web app works without a Python HTTP backend. The backend is only needed for features that depend on live server-side state, including:

- recommendations
- analytics exports and summaries
- write-side search logging

## Typical Workflow

1. Open `/` to read recent posts and search the article library.
2. Switch to **Catalog** mode when you want to change which sources are included.
3. Use `/dashboard` when you want collection health.
4. Use repo commands when you want OPML files, analytics exports, or generated artifacts.
