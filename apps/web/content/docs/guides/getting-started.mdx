---
title: Getting Started
description: Install the repo, run the web app, and generate the article library with the required uv and pnpm workflows.
icon: Rocket
---

import { Step, Steps } from "fumadocs-ui/components/steps";

This guide takes you from a fresh clone to a working local setup.

## Prerequisites

- Python 3.13+
- Node.js 20+
- [`uv`](https://docs.astral.sh/uv/)
- [`pnpm`](https://pnpm.io/)
- Git

<Steps>
  <Step>

### Clone the repo

```bash
git clone https://github.com/wyattowalsh/ai-web-feeds.git
cd ai-web-feeds
```

  </Step>
  <Step>

### Install Python dependencies

```bash
uv sync
```

  </Step>
  <Step>

### Install the web app dependencies

```bash
cd apps/web
pnpm install
cd ../..
```

  </Step>
  <Step>

### Build the article library

```bash
uv run ai-web-feeds corpus refresh
```

This populates the runtime data and writes `data/articles.generated.json`, which powers the main Feeds experience.

  </Step>
  <Step>

### Start the web app

```bash
cd apps/web
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) and start in `/feeds`.

  </Step>
</Steps>

## Useful Next Commands

```bash
uv run ai-web-feeds validate http
uv run ai-web-feeds enrich all \
  --input data/feeds.yaml \
  --output data/feeds.enriched.yaml \
  --schema data/feeds.enriched.schema.json \
  --database sqlite:///data/ai-web-feeds.db
```

## Where to Go Next

- [Feeds](/docs/features/reader)
- [Search and Discovery](/docs/features/search)
- [Deployment](/docs/guides/deployment)
- [Testing](/docs/guides/testing)
