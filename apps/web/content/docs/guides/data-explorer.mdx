---
title: Source Catalog and Reader
description: Current source, topic, reader, and removed explorer route reference.
---

# Source Catalog and Reader

The former public Data Explorer route at `/explorer` has been removed. Use the current public
surfaces instead:

- [`/reader`](/reader) for recent posts across the source registry
- [`/sources`](/sources) for source search, type filters, topic filters, and OPML export
- [`/topics`](/topics) for public topic landing pages
- [`/dashboard`](/dashboard) for compact catalog health

## Current Data Surfaces

The same repository data remains available through the web app and source files:

- `data/feeds.yaml` and `data/feeds.enriched.yaml` define the source catalog
- `data/topics.yaml` defines the topic taxonomy
- `data/articles.generated.json` powers the standalone reader and article search
- `GET /api/feeds`, `GET /api/topics`, and `GET /api/exports/opml` expose catalog data to the web app

## Related Pages

- [Reader](/docs/features/reader)
- [Search & Discovery](/docs/features/search)
- [Feed Schema](/docs/guides/feed-schema)
- [Runtime Authority](/docs/development/runtime-authority)
