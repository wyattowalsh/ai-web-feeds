---
title: Search Architecture
description: How the web app combines the checked-in catalog with the generated article corpus for reader-first browse, search, and autocomplete
---

import { Callout } from "fumadocs-ui/components/callout";

# Search Architecture

The current public search model is intentionally split between two local data
sources:

- the **checked-in source catalog** for feed discovery
- the **generated article corpus** for article browse, article search, and
  autocomplete article suggestions

The primary product route is `/feeds`, and the reader-first workspace should
work without a required Python HTTP backend.

## Public search surfaces

| Route | Backing data | Purpose |
| --- | --- | --- |
| `/api/articles` | `data/articles.generated.json` | filtered article browse with cursor pagination |
| `/api/search?scope=articles` | `data/articles.generated.json` | ranked article search across the exported corpus |
| `/api/search?scope=sources` | checked-in catalog files | source discovery and filtering |
| `/api/search/autocomplete` | shared catalog + article corpus index | feed, article, and topic suggestions |
| `POST /api/search` | optional backend proxy | analytics logging only |

All GET routes above are served from the Next.js app and set explicit cache
headers. The POST logging path is the only search operation that still depends
on `BACKEND_URL`.

## Search flow

```mermaid
flowchart LR
    authored[data/feeds.yaml + data/topics.yaml] --> enriched[data/feeds.enriched.yaml]
    runtime[(feed_entries in data/ai-web-feeds.db)] --> corpus[data/articles.generated.json]
    enriched --> sourceIndex[source catalog index]
    corpus --> articleIndex[article corpus index]
    sourceIndex --> sourceSearch[/api/search?scope=sources]
    articleIndex --> browse[/api/articles]
    articleIndex --> articleSearch[/api/search?scope=articles]
    sourceIndex --> auto[/api/search/autocomplete]
    articleIndex --> auto
    browse --> feeds[/feeds]
    articleSearch --> feeds
    sourceSearch --> feeds
    auto --> feeds
```

## Shared article corpus behavior

`apps/web/lib/article-corpus.ts` is the shared server-only adapter for:

- loading `data/articles.generated.json`
- normalizing article filters (`feed`, `topics`, `source_type`, `verified`)
- applying browse sort order (`latest`, `oldest`, `source`)
- serving article search and autocomplete from the same normalized article rows

That means article browse, article search, and article autocomplete suggestions
now share one authoritative dataset instead of mixing bounded live fetches and
catalog-only matching.

## Source catalog behavior

Source search still uses the repository catalog because feed discovery is an
authored-data concern.

The web loader prefers these files in order:

1. `data/feeds.enriched.yaml`
2. `data/feeds.yaml`
3. `data/feeds.json`

This keeps source discovery available even when the runtime database or corpus
has not been refreshed yet.

<Callout type="info" title="Operational distinction">
  If source search looks correct but article browse/search is empty, check the
  generated corpus path before changing catalog files. The reader workspace
  depends on `data/articles.generated.json`, not on direct SQLite access.
</Callout>

## Runtime search stack

The Python runtime still supports its own search stack for CLI and backend-side
operations:

| Layer | Implementation | Purpose |
| --- | --- | --- |
| Full-text | SQLite FTS5 virtual table + triggers | source search over stored runtime data |
| Autocomplete | trie index built from titles and topics | fast prefix suggestions |
| Semantic | embeddings + cosine similarity | meaning-based retrieval |

Those runtime capabilities are still useful for CLI workflows, but they are no
longer a prerequisite for the public `/feeds` experience.

## Search operations checklist

```bash
# Build the runtime article rows and export the generated web corpus
uv run ai-web-feeds corpus refresh

# Re-export from the current runtime DB without polling
uv run ai-web-feeds corpus export

# Optional runtime search/index workflows for CLI and backend usage
uv run ai-web-feeds search init
uv run ai-web-feeds search query "llm agents" --type full_text --limit 10
uv run ai-web-feeds search autocomplete llm --limit 8
uv run ai-web-feeds search embeddings --provider local
```

## Data dependencies

- source discovery depends on checked-in catalog files
- article browse/search/autocomplete article suggestions depend on the generated
  corpus artifact
- the generated corpus depends on `feed_entries` in the runtime database
- search logs and saved searches remain runtime data scoped to the anonymous
  device binding, not authored YAML

## Related documentation

- [Runtime authority](/docs/development/runtime-authority)
- [Search & Discovery](/docs/features/search)
- [Reader](/docs/features/reader)
- [Deployment Guide](/docs/guides/deployment)
