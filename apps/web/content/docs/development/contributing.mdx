---
title: Contributing
description: Contribute to ai-web-feeds using the repository-approved uv and pnpm workflow, with links to the deeper contribution guides.
---

import { Card, Cards } from "fumadocs-ui/components/card";

# Contributing

Thank you for your interest in contributing to ai-web-feeds. This page is the main contributor entry point and links to the deeper workflow guides that support the repository.

## Quick Start

Use the repository-standard toolchain from the start:

```bash
git clone https://github.com/wyattowalsh/ai-web-feeds.git
cd ai-web-feeds

uv sync

cd apps/web
pnpm install
cd ../..

uv run pre-commit install
uv run pre-commit install --hook-type commit-msg
uv run pytest
uv run ai-web-feeds --help
```

## Required Tooling

- Python commands use `uv` only.
- Node.js commands use `pnpm` only.
- Project documentation belongs in `apps/web/content/docs/` as `.mdx` files.

## Contribution Guides

These pages contain the detailed workflows that are now linked from the visible docs tree:

<Cards>
  <Card title="Development Workflow" href="/docs/contributing/development-workflow" description="Environment setup, branch strategy, testing flow, and day-to-day development commands." />
  <Card title="Pre-commit Hooks" href="/docs/contributing/pre-commit-hooks" description="Hook installation, local troubleshooting, and how the same checks run in CI." />
  <Card title="Conventional Commits" href="/docs/contributing/conventional-commits" description="Commit message structure, scopes, and optional interactive commit helpers." />
</Cards>

## Documentation Notes

Documentation is built with Fumadocs and lives in `apps/web/content/docs/`.

When you add or move docs pages:

1. Create or update the `.mdx` file in the correct section.
2. Keep `apps/web/content/docs/meta.json` aligned with the visible docs graph.
3. Add `title` and `description` frontmatter so the page renders cleanly in navigation and metadata.
4. Spot-check `/llms.txt`, `/llms-full.txt`, and one `.mdx` route when the change affects docs structure or route examples.

For the full docs-health workflow, use the [Docs Maintenance Checklist](/docs/contributing/docs-maintenance).

## Common Next Steps

- For architecture and implementation context, continue to [Development](/docs/development).
- For deployment and environment details, see [Deployment](/docs/guides/deployment).
- For issue tracking and discussion, use [GitHub Issues](https://github.com/wyattowalsh/ai-web-feeds/issues) and [GitHub Discussions](https://github.com/wyattowalsh/ai-web-feeds/discussions).

## Repository Notes

The repository still supports a legacy SQLite filename, `data/aiwebfeeds.db`, for older local installs. New examples and setup steps should use `data/ai-web-feeds.db`.
